apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: azuresql-claim0
  name: azuresql-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
