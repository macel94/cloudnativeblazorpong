apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: collector-claim0
  name: collector-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
