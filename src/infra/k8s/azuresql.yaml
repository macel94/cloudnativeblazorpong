apiVersion: apps/v1
kind: Deployment
metadata:
  name: azuresql
  namespace: blazorpong
  labels:
    app.kubernetes.io/name: azuresql
    app.kubernetes.io/part-of: blazorpong
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: azuresql
  template:
    metadata:
      labels:
        app.kubernetes.io/name: azuresql
        app.kubernetes.io/part-of: blazorpong
    spec:
      containers:
        - name: azuresql
          image: mcr.microsoft.com/mssql/server:2025-latest
          imagePullPolicy: IfNotPresent
          env:
            - name: ACCEPT_EULA
              value: "Y"
            - name: MSSQL_SA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: azuresql-credentials
                  key: MSSQL_SA_PASSWORD
          ports:
            - containerPort: 1433
              protocol: TCP
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - /opt/mssql-tools18/bin/sqlcmd -S '127.0.0.1,1433' -C -U sa -P "$MSSQL_SA_PASSWORD" -Q "SELECT 1" -No
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 15
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - /opt/mssql-tools18/bin/sqlcmd -S '127.0.0.1,1433' -C -U sa -P "$MSSQL_SA_PASSWORD" -Q "SELECT 1" -No
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: azuresql
  namespace: blazorpong
  labels:
    app.kubernetes.io/name: azuresql
    app.kubernetes.io/part-of: blazorpong
spec:
  selector:
    app.kubernetes.io/name: azuresql
  ports:
    - name: mssql
      port: 1433
      targetPort: 1433
