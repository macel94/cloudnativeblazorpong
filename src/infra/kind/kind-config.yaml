# Kind cluster configuration for cloudnativeblazorpong
# Maps container port 30080 to host port 8080 for Gateway API access
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
