apiVersion: apps/v1
kind: Deployment
metadata:
  name: azuresql
  namespace: blazorpong
  labels:
    app: azuresql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: azuresql
  template:
    metadata:
      labels:
        app: azuresql
    spec:
      containers:
        - name: azuresql
          image: mcr.microsoft.com/mssql/server:2025-latest
          imagePullPolicy: IfNotPresent
          env:
            - name: ACCEPT_EULA
              value: "Y"
            - name: MSSQL_SA_PASSWORD
              value: "yourStrong(!)Password"
          ports:
            - containerPort: 1433
              protocol: TCP
          readinessProbe:
            exec:
              command:
                - /bin/bash
                - -c
                - /opt/mssql-tools18/bin/sqlcmd -S 127.0.0.1 -C -U sa -P "$MSSQL_SA_PASSWORD" -Q "SELECT 1"
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 15
---
apiVersion: v1
kind: Service
metadata:
  name: azuresql
  namespace: blazorpong
  labels:
    app: azuresql
spec:
  ports:
    - name: mssql
      port: 1433
      targetPort: 1433
  selector:
    app: azuresql
